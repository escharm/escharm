"use client";

import TopMenu from "@/components/TopMenu";

const Page = () => {
  return (
    <div>
      <TopMenu />
      <button onClick={async () => {}}>test</button>
    </div>
  );
};

export default Page;
